#!/usr/bin/env sh

set -e
. "./bin/functions"
test -f .envrc && . "./.envrc"

pp_info "build" "Building the frontend assets..."

rm -rf frontend-svelte/build priv/static
yarn build
mkdir -p priv/static
cp -R frontend-svelte/build/* priv/static
rm -rf frontend-svelte/build
